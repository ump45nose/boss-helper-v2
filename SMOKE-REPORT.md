# Boss Helper V2 受控冒烟报告

日期：2026-08-20

## 已执行

- 基线确认：HEAD 为 `ad47412a44b59a57546200ab26f1f4b78c4bb4e4`，版本更新为 `0.6.0`。
- `npm run check`：通过。
- `npm run lint`：通过；仅有官方基线既存的非阻断 warning。
- `npm run build:chrome`：通过，生成 `.output/chrome-mv3/manifest.json`。
- `npm run zip:chrome`：通过，ZIP 根目录包含 `manifest.json`，并包含独立 chat-monitor content script。
- `npm run smoke:v2`：通过，校验 Manifest MV3/0.6.0、ZIP 根目录和画像字段。
- 定向 `oxfmt --check`：通过；全仓检查仍受 `.claude/skills` 等基线文档未格式化影响。
- 批次长等待：静态确认只按实际 `岗位投递` 成功计数，X～Y 阈值和 60～240 秒等待范围经过配置归一化，停止/每日上限可中断等待。
- 图片简历：静态确认默认关闭、IndexedDB 本机保存、PNG/JPEG/WebP 与 2 MiB 限制、文本招呼语成功后的顺序发送、失败停止后续岗位和配置导出脱敏。
- 兜底招呼语：静态确认默认关闭、文本长度/句数校验、AI 未配置/异常/无效输出路径可使用兜底；BOSS 发布或确认超时后不自动重发。
- 高级招呼图片：静态确认自定义招呼语数组中的图片按配置顺序复用上传与 MQTT/protobuf 发送链路，消息不使用 retained flag。
- 岗位列表 UI：静态确认 `JobCards.vue` 使用语义化 `<table>` 文字行，不再挂载旧 `JobCard.vue`，并保留详情展开、状态筛选和键盘操作。
- 关于页：静态确认已移除“关于&赞赏”入口、页面组件和远程收款码引用。
- 筛选状态：静态确认会在 DOM 变化、滚动和窗口尺寸变化时重新识别官方筛选容器；无法安全取得稳定属性时保持保存/恢复停用。
- 交付 ZIP SHA256：`E25FA2848B817F8780EF6FD72651F27359AE94025E488E6AEDCDF949BF92B5BE`。
- 静态边界检查：确认日志脱敏、模型密钥只走 V2 local namespace、模型导出移除密钥、回复监控和“自动投递（含招呼语）”默认关闭，Manifest 不申请 Cookie 权限，交付物不含 API 密钥或完整聊天样本。
- 诊断链路：静态确认“详细诊断日志（仍脱敏）”默认关闭，开启后只允许阶段、耗时、超时配置、错误分类等白名单摘要；模型超时统一为 120 秒默认值，并限制在 5 秒至 10 分钟。

## 未执行

- 未使用真实 BOSS 会话，未导出或展示 Cookie，未进行真实投递、批量发送或自动回复。
- 未模拟 Chrome 通知点击和 BOSS DOM 交互；这些需要用户本人在已登录页面按受控步骤确认。
- 未声称“防封号”；随机等待和退避只用于保守限流。
- 批次长等待范围在关闭开关时仍可编辑，普通延迟使用固定值上下 20% 的有界随机抖动；未实现随机点击、滚动、输入轨迹或会话进出等拟人化事件。
- 图片简历发布前复用消息发送的有界等待；未新增页面点击、滚动或会话进出动作。
- 系统默认招呼语路径：静态确认 AI/自定义招呼停用时仍挂载独立图片简历任务，已发送图片时按状态去重；未使用真实账号发送。
- 岗位间隔：静态确认默认投递间隔已调整为 15 秒，运行时按 ±20% 有界随机等待约 12～18 秒；旧版未自定义的 5 秒配置迁移为 15 秒，页面修改后的标量不会被历史等值范围覆盖。

## 已知限制

- WXT 构建阶段仍会输出 Nuxt UI 的 `SalaryRange` 命名冲突提示和 `EMPTY_IMPORT_META` 非阻断警告；产物构建成功，警告不包含凭据或页面内容。
- 回复监控首版默认只保存待人工确认队列；只有用户点击“生成 AI 草稿”时才由 background/service worker 调用模型，仍不会自动填入或点击发送。
- 本轮未开启“自动投递（含招呼语）”、批次长等待、兜底招呼语或图片简历发送，未用真实账号调用投递或聊天发送；这些功能仅完成默认关闭、配置入口、发送顺序和 fail-closed 守卫的静态验证。
- 未用真实模型执行超时回归；模型服务慢、无流式首块或服务端主动断开时，需在用户确认后开启诊断摘要并只运行单个岗位。
