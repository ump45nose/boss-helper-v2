# 隐私政策 - BossHelper（Boss直聘 AI 求职助手）

最后更新：2026年8月23日

本扩展仅在 `zhipin.com` 及其子域运行，用于帮助用户筛选职位、辅助投递、生成可选招呼语和显示处理结果。不使用 `chrome.cookies` API，也不会导出或展示 Boss直聘 Cookie。

我们收集/处理的数据：

- 求职网站内容：职位、公司、招聘者、职位详情、投递状态及用户在 Boss直聘页面上的相关操作数据，仅用于本地筛选、去重、展示和通过页面已有会话发起用户明确开启的投递或聊天请求。
- 用户自愿输入的 API Base URL 和 API Key：仅用于调用用户指定的 AI 服务，保存在 V2 独立的 `chrome.storage.local` namespace，不写入源码、日志、导出文件或 ZIP。
- 用户自愿输入的候选人事实画像和待确认草稿：保存在 V2 独立的 `chrome.storage.local` namespace；不会写入日志或导出文件中的敏感字段。
- 用户主动上传的图片简历：图片二进制只保存在本机扩展 IndexedDB，配置仅保存本地引用的名称和类型；不会发送给 AI 服务，不写入日志、配置导出或 ZIP。用户可在配置页清除。
- 用户填写的兜底招呼语：仅作为本机配置保存在 V2 独立的 `chrome.storage.local` namespace；发送时仅在用户开启自动投递且 AI 尚未产生外部消息时使用，正文不会写入任务日志或统计。
- 页面任务日志和统计：只保存脱敏的阶段、计数、时间和错误分类，不保存 Cookie、密钥、完整 Prompt、完整模型响应或完整聊天全文。
- 可选 AI、地图和公告服务：用户启用 AI 功能后，必要的职位、招聘者文本和用户输入会发送给用户自行选择的 AI 服务；启用距离或路线筛选时，必要的地址或坐标会发送给高德地图；启动时会从 jsDelivr 获取公开的版本与公告配置。

数据只会在提供用户主动启用的功能所必需时发送给 Boss直聘、用户选择的 AI 服务、高德地图或 jsDelivr。我们不出售用户数据，不将数据用于广告、画像或与扩展单一用途无关的目的。

用户控制：

- 可随时在扩展选项中删除/修改 API Key、候选人画像和草稿队列。
- 卸载扩展即删除所有本地数据。

无其他浏览活动收集。扩展仅在 zhipin.com 域名注入脚本；回复监控默认关闭，开启后只发送通知并打开会话，不自动回复后续消息。岗位配置中的“自动投递（含招呼语）”默认关闭，只有用户明确开启后，筛选通过的岗位才会使用页面已有投递与聊天通道发送文本或用户配置的高级招呼语图片；AI 兜底语需要单独开启，图片简历还需要单独开启，且只在尚未发送外部消息的生成阶段或最终招呼语成功后按顺序尝试，不自动交换联系方式。“详细诊断日志（仍脱敏）”默认关闭，开启后只保存阶段、耗时、超时配置和错误分类等白名单摘要，强制排除 API 密钥、Cookie、Prompt、模型响应、聊天全文和请求头/请求体。

如有疑问：联系 @Ocyss_04 或 issues。
