# 更新日志

## 0.6.0 - 2026-08-05

- 修复批次长等待范围在关闭开关或重载旧配置时不可编辑/显示为空的问题；增加普通延迟与批次等待策略的可见说明。
- 明确不实现随机点击、滚动、输入轨迹或会话进出等拟人化事件，仅保留有界等待和安全限流。
- 修复 AI/自定义招呼停用时图片简历不发送的问题：沿用 BOSS 系统默认招呼语，并在其后追加已配置图片。
- 基于官方 `ad47412a44b59a57546200ab26f1f4b78c4bb4e4` 创建独立 V2 扩展身份，可与官方扩展并行安装。
- 增加脱敏持久化日志、岗位列表阶段/错误展示和幂等事件统计，零分母不再显示 `NaN`。
- 投递和消息等待支持可配置范围及有界随机抖动；增加每日上限、连续错误退避和登录/验证码/限流 fail-closed。
- 将岗位筛选和 AI 招呼语 Prompt 替换为事实约束、结构化输出和人工判断策略。
- 增加本地 `candidate-profile.v1` 画像编辑与迁移；画像、模型密钥仅保存在 V2 本机存储。
- 筛选状态按页面路径保存/恢复，DOM 契约不匹配时不猜测选择器、不搬运 BOSS 原生节点。
- 将岗位展示改为语义化文字表格行，移除岗位卡片背景、头像和倾斜布局；回复监控仅提供用户主动开启的浏览器通知和待确认草稿队列。
- 删除“关于&赞赏”入口、页面和收款码，不再加载远程赞赏图片；筛选契约无法匹配时改为明确显示保存/恢复已停用。
- 筛选页增加“定位 BOSS 原生筛选”和当前条件只读摘要；原生自定义下拉缺少稳定属性时不伪造保存/恢复成功。
- 增加默认关闭的“自动投递（含招呼语）”开关；关闭时流程停在待人工，开启后才允许既有 BOSS 投递流程和文本招呼语发送。
- 增加默认关闭的“投递批次长等待”：在 X～Y 次实际成功投递后随机等待 1～4 分钟，支持每日上限和停止条件优先中断；不承诺防封号，也不提供代理轮换、指纹伪装或验证码绕过。
- 增加默认关闭的“招呼语后发送图片简历”：用户上传不超过 2 MiB 的 PNG/JPEG/WebP 后可开启，只有文本招呼语发送成功才尝试发送图片；图片仅存本机 IndexedDB，不进入 AI、日志、导出或 ZIP，失败停止后续岗位且不自动重试。
- 修复图片简历配置升级后引用被清空的问题；恢复高级招呼语中图片消息的按序发送，并禁止 MQTT retained message，避免重连后重复展示。
- 增加默认关闭、可配置的 AI 兜底招呼语；模型初始化/请求失败、超时、无效输出或“需人工判断”时可生成兜底草稿/消息，BOSS 消息已发布或确认超时后不自动重发。
- 自动投递沿用页面已有 BOSS 聊天通道；继续禁止自动回复、自动交换联系方式、代理轮换、指纹伪装和验证码绕过。图片简历发送是单独的高风险开关，不会被自动投递开关隐式开启。
- 将模型默认超时统一为 120 秒并限制在 5 秒至 10 分钟；模型超时统一显示安全分类，不再把 `signal timed out` 原文直接扩散到任务日志。
- 增加默认关闭的“详细诊断日志（仍脱敏）”选项，记录 AI 阶段、耗时、超时配置和错误分类；无论开关状态都不记录 API 密钥、Cookie、Prompt、模型响应或聊天全文。
