# 贡献指南

感谢你对本项目的关注和贡献。

本项目是一个基于 **Bun、WXT、Vue、Nuxt UI、Tailwind CSS** 的浏览器扩展项目，主要用于 Boss 直聘相关的自动化投递、网页交互与功能增强。

由于项目涉及浏览器扩展、网页自动化以及对目标网站行为的适配，代码本身不仅需要“能够运行”，还需要考虑后续维护成本、目标网站变化以及浏览器兼容性。

因此，本项目对贡献的核心要求并不是“代码越多越好”，而是：

> **解决真正的问题，用尽可能少的修改，保持代码简单，并在实现较大功能之前先进行讨论。**

---

## 核心原则

请在提交 Issue 或 Pull Request 之前，先理解以下原则。

### 1. 小而专注

**一个 PR 尽量只解决一个问题。**

例如：

- 修复一个 Bug → 一个 PR
- 增加一个独立功能 → 一个 PR
- 优化一个明确的代码逻辑 → 一个 PR
- 修改一个 UI 问题 → 一个 PR
- 独立的重构 → 单独一个 PR

不要把多个互不相关的修改放在同一个 PR 中。

例如下面这种 PR 不推荐：

```text
修复职位列表 Bug
+ 增加自动刷新
+ 增加新的筛选功能
+ 重构职位数据结构
+ 修改登录逻辑
+ 更新依赖
+ 修改 UI
+ 增加日志
+ 顺便修复其他几个 Bug
```

即使这些修改本身都没有明显错误，也会显著增加 Review 和维护成本。

更推荐拆成：

```text
PR #1：修复职位列表 Bug

PR #2：增加自动刷新

PR #3：优化职位数据结构

PR #4：增加新的筛选功能
```

这样每个 PR 都更容易理解、Review、回滚和维护。

---

### 2. 改动规模应该与问题规模匹配

本项目**不禁止大型 PR**。

如果一个功能确实需要修改几十个文件，可以提交大型 PR。

但是：

> **PR 的修改规模必须能够被问题本身合理解释。**

例如：

一个简单的条件判断 Bug：

```diff
- if (job.status === 'normal') {
+ if (job.status === 'normal' && !job.deleted) {
```

如果最终 PR 变成：

```text
修改 47 个文件
新增 1800 行
删除 1300 行
新增 12 个工具函数
重构 5 个模块
升级 8 个依赖
```

那么需要重新检查：

> 这些修改真的都是修复这个问题所必需的吗？

如果不是，请拆分。

---

### 3. 先讨论，再实现

对于**新功能、较大的优化、架构调整、行为变化**，请先创建 Issue 讨论。

不要：

> “我觉得这个功能很好 → 花三天写完 → 提交 PR → 等维护者决定要不要。”

更推荐：

```text
Issue
  ↓
讨论需求
  ↓
确认是否需要
  ↓
讨论实现方向
  ↓
维护者认可方案
  ↓
开始实现
  ↓
提交 PR
```

这样做并不是为了增加流程，而是为了避免贡献者投入大量时间实现一个项目实际上并不需要的功能。

维护者可能已经：

- 正在考虑这个问题
- 已经知道问题的原因
- 已经有其他实现方案
- 计划在其他模块解决
- 认为这个功能没有必要
- 认为这个功能增加的维护成本大于收益

在这些情况下，如果提前讨论，往往几分钟就可以避免贡献者数小时甚至数天的无效工作。

---

## 什么情况下必须先创建 Issue？

以下情况请**先创建 Issue 并进行讨论**：

- 新增功能
- 修改现有功能的行为
- 大型重构
- 修改项目架构
- 替换核心依赖
- 新增较重的第三方依赖
- 修改数据结构
- 修改核心 API
- 大规模 UI 改动
- 修改自动化流程
- 修改与 Boss 直聘网页行为相关的核心逻辑
- 任何你认为可能影响项目整体设计的修改

特别是：

> **如果你不确定维护者是否希望这个功能存在，请先开 Issue，不要直接实现。**

---

## Issue 获得讨论后才能实现

创建 Issue 并不意味着你自动获得了实现许可。

对于较大的功能，请等待维护者明确认可需求和实现方向后再开始实现。

例如：

```text
贡献者：

我希望增加一个“自动根据职位描述生成求职话术”的功能。

维护者：

这个方向可以考虑，但我希望生成逻辑放在现有 AI 模块中，
而不是新增一个独立服务。

贡献者：

了解，我会按照这个方向实现。
```

这时候再开始写代码。

而不是：

```text
贡献者：

我觉得这个功能应该这样做。

[直接实现 2000 行]

PR：

已经实现，请 Review。
```

后者很可能导致大量重复工作。

---

## Pull Request 要求

## 1. PR 应该保持单一目的

一个 PR 应该能够用一句话说明：

> “这个 PR 解决了什么问题？”

例如：

```text
修复职位详情页无法正确识别已投递职位的问题。
```

而不是：

```text
优化职位模块并增加一些新功能。
```

如果一句话无法清楚描述 PR 的目的，通常说明 PR 的范围可能过大。

---

## 2. PR 标题应该清晰

推荐：

```text
fix: 修复职位详情页状态识别错误

feat: 增加自动暂停投递功能

refactor: 简化职位状态判断逻辑

fix: 修复 Firefox 下消息通信异常
```

不推荐：

```text
update

fix

优化

一些修改

最新版

changes

AI generated changes
```

---

## 3. PR 描述应该说明修改内容

推荐至少说明：

```markdown
## 问题

描述原本存在的问题。

## 解决方案

说明采用了什么方式解决。

## 修改范围

说明修改了哪些内容。

## 验证方式

说明如何验证修改有效。

## 其他修改

说明是否存在与主要问题无关的修改。
```

尤其需要注意：

### “其他修改”

如果 PR 中存在与主要问题无关的修改，请明确说明。

例如：

```markdown
## 其他修改

无。

本 PR 没有修改其他无关代码。
```

或者：

```markdown
## 其他修改

由于新的 API 需要更新类型定义，因此同时修改了
src/types/job.ts。

除此之外没有其他无关修改。
```

---

## 不要“顺手重构”

这是本项目非常重要的一条规则。

如果你在修改某个问题时发现：

> “这里的代码写得不太好，我顺便重构一下。”

请先停下来。

如果重构与当前问题无关，请不要放进当前 PR。

例如你正在修复：

```ts
if (job.status === 'available') {
  apply(job)
}
```

然后发现整个职位模块都可以重构。

不要把 PR 变成：

```text
修复一个职位状态 Bug

↓ 顺便

重构 Job 类型
重构 JobService
重构状态管理
重构 API
重命名几十个变量
移动文件
修改目录结构
```

如果这个重构确实有价值：

> **请单独创建 Issue / PR。**

本项目非常欢迎有价值的代码优化，但希望它们与具体问题分离。

这样维护者可以独立判断：

> “这个 Bug 修复需要合并。”

以及：

> “这个重构是否值得承担额外维护成本。”

---

## 简单优先，避免过度抽象

本项目更倾向于：

> **简单、直接、容易理解的代码。**

而不是为了形式上的“优雅”“复用”“工程化”增加额外抽象。

## 不要为了只使用一次的逻辑创建函数

例如：

```ts
if (job.status === 'available' && !job.isExpired) {
  applyJob(job)
}
```

如果这个判断只使用一次，通常没有必要改成：

```ts
if (isAvailableJob(job)) {
  applyJob(job)
}
```

再进一步：

```ts
function isAvailableJob(job: Job) {
  return hasValidStatus(job) && isNotExpired(job)
}

function hasValidStatus(job: Job) {
  return job.status === 'available'
}

function isNotExpired(job: Job) {
  return !job.isExpired
}
```

这种代码看起来“结构清晰”，实际上可能让代码更难理解。

阅读原始代码时：

```ts
job.status === 'available' && !job.isExpired
```

信息是直接可见的。

而抽象之后：

```ts
isAvailableJob(job)
```

读者必须继续跳转：

```text
isAvailableJob
    ↓
hasValidStatus
    ↓
isNotExpired
```

才能知道真正的判断条件。

---

## 抽象应该来自真实需求

并不是说本项目禁止函数、工具函数或抽象。

如果某段逻辑：

- 被多个地方使用
- 有明确的业务含义
- 本身比较复杂
- 需要独立测试
- 需要隐藏复杂实现
- 独立出来明显提高可读性

那么应该进行抽象。

例如：

```ts
const expired = isJobExpired(job)
```

如果 `isJobExpired()` 在多个模块中使用，而且判断逻辑比较复杂，那么抽象就是合理的。

核心区别是：

> **不要为了抽象而抽象。**

---

## 避免“AI 风格”的过度工程化

AI 生成代码经常倾向于：

- 大量创建工具函数
- 创建只使用一次的函数
- 创建不必要的类型
- 创建多层 wrapper
- 创建过度复杂的配置
- 创建不必要的 interface
- 创建不必要的 class
- 增加不必要的错误处理层
- 增加未来可能使用的扩展点
- 将简单逻辑拆成很多小文件

这些修改并不会因为“看起来更工程化”就自动变得更好。

提交代码前请问自己：

> 如果不做这个抽象，代码真的会更难维护吗？

如果答案是否定的，那么通常不需要增加它。

---

## 避免无意义注释

注释应该解释：

> **为什么这样做。**

而不是解释：

> **代码正在做什么。**

不推荐：

```ts
// Check if the job exists
const exists = jobs.some((job) => job.id === id)
```

代码本身已经说明了这件事。

不推荐：

```ts
// Get the job
const job = getJob(id)
```

同样没有提供额外信息。

---

## 推荐解释“为什么”

例如：

```ts
// Boss 直聘这里可能返回已经失效的职位，但状态字段仍然保持正常。
// 因此不能只依赖 status 判断职位是否有效。
if (job.status === 'available' && !job.deleted) {
  // ...
}
```

这种注释具有长期维护价值。

尤其是逆向、网页自动化相关代码，如果某个奇怪的判断、延迟、兼容性处理是因为目标网站的特殊行为，那么非常值得注释原因。

例如：

```ts
// 这里不能立即读取元素。
// Boss 直聘会在 DOM 插入后异步更新职位信息，需要等待下一次渲染。
await nextTick()
```

这种注释非常有价值。

---

## 不要为“未来可能需要”提前设计

不要因为：

> “以后可能会支持……”

就提前增加大量抽象。

例如：

```ts
interface JobProvider {
  fetchJobs(): Promise<Job[]>
}

class BossProvider implements JobProvider {}

class FutureProvider implements JobProvider {}

class ProviderFactory {}

class ProviderManager {}
```

如果当前项目只有 Boss 直聘，并且没有实际需求支持其他 Provider，这种架构可能只是增加维护成本。

遵循：

> **先解决当前真实存在的问题。**

未来真的出现需求时，再根据真实需求设计抽象。

---

## 不要为了“代码规范”增加复杂度

不要为了满足某种理论上的代码规范而：

- 强制所有判断提取函数
- 强制所有字符串创建常量
- 强制所有逻辑拆成多个函数
- 强制所有模块创建 interface
- 强制所有组件拆成多个文件
- 强制所有逻辑创建 composable
- 强制所有逻辑创建 service
- 强制增加 wrapper

项目代码风格应该服务于：

> **可读性、简单性和长期维护。**

而不是为了满足某种“看起来更专业”的代码结构。

---

## AI / Vibe Coding

## 本项目允许使用 AI

本项目**不反对 AI Coding、Vibe Coding、Copilot、Claude、ChatGPT、Cursor、Codex 等工具**。

你可以自由使用 AI：

- 编写代码
- 查找问题
- 分析代码
- 编写测试
- 生成文档
- 重构代码
- 辅助逆向分析

本项目不会因为代码使用了 AI 就拒绝贡献。

---

## 但是，贡献者必须对提交的代码负责

无论代码是：

```text
自己手写
AI 辅助
AI 生成
Vibe Coding
复制后修改
```

最终提交 PR 的人都必须对代码负责。

这意味着：

> **不要提交自己无法理解的代码。**

如果维护者询问：

```text
为什么这里需要这样处理？
```

贡献者应该能够解释。

如果维护者询问：

```text
为什么这里需要新增这个依赖？
```

贡献者应该能够解释。

如果维护者询问：

```text
为什么这个函数必须拆出来？
```

贡献者应该能够解释。

---

## 不需要主动披露是否使用 AI

本项目不要求贡献者在 PR 中声明：

```text
我使用了 ChatGPT。
```

也不要求：

```text
AI generated: true
```

我们关心的是：

> **最终代码是否合理，而不是代码是由谁输入键盘产生的。**

AI 可以帮助你提高效率。

但：

> **AI 的生成结果不是代码质量的保证。**

---

## AI 生成代码尤其需要注意

提交 AI 辅助生成的代码之前，请主动检查：

### 是否产生了不必要的抽象？

```ts
function isValidJob(job: Job) {
  return job.status === 'available'
}
```

如果只使用一次，并且判断非常简单，可能没有必要。

---

### 是否生成了不必要的注释？

```ts
// This function handles the job application process.
function applyJob() {
  // ...
}
```

如果注释没有提供额外信息，可以删除。

---

### 是否修改了不相关的代码？

例如你让 AI 修复一个 Bug，结果它：

```text
修改 25 个文件
重构 8 个函数
新增 4 个工具类
更新依赖
修改格式
```

请不要直接提交。

先检查：

> 哪些修改真正与这个 Bug 有关？

然后删除其他修改。

---

### 是否引入了不必要的依赖？

AI 很容易因为“方便”而引入一个新的 npm 包。

在添加依赖之前，请确认：

> 这个依赖真的有必要吗？

如果几行简单代码就能完成，就不要为了方便增加新的依赖和长期维护成本。

---

## 依赖管理

请谨慎添加新的 npm 依赖。

添加依赖时，应考虑：

- 是否真的需要？
- 现有依赖是否已经提供类似能力？
- 是否可以用少量代码实现？
- 这个依赖是否长期维护？
- 是否会增加构建体积？
- 是否会影响浏览器扩展兼容性？
- 是否会增加未来升级成本？

不要为了一个非常小的功能增加一个大型依赖。

---

## 不要顺手升级依赖

如果你的 PR 是：

```text
fix: 修复职位列表错误
```

请不要顺便：

```text
升级 Bun
升级 WXT
升级 Vue
升级 Nuxt UI
升级 Tailwind CSS
更新几十个 npm 包
```

除非这些升级确实是解决问题所必需的。

如果发现某个依赖确实需要升级，请单独提出 Issue / PR，或者在 PR 中明确说明原因。

---

## 不要提交无关的格式化修改

不要因为修改一个文件就顺便：

```text
格式化整个项目
修改整个目录的缩进
修改换行符
重新排序所有 import
修改大量无关代码格式
```

这会严重增加 Review 成本。

尤其不要让一个 Bug Fix 变成：

```diff
+ 100
- 100
```

但实际逻辑只修改了两行。

---

## 浏览器扩展项目的测试与验证

本项目涉及：

- 浏览器 API
- WXT
- Vue
- Shadow DOM
- 网页 DOM
- 真实网站行为
- 网络请求
- 浏览器环境
- 逆向分析

因此，并不要求所有修改都提供单元测试。

**本项目不会为了“测试覆盖率”而强制要求为所有代码编写单元测试。**

---

## 但必须进行合理验证

如果修改的是可以手动验证的功能，请尽可能进行真实环境验证。

例如：

```markdown
## 验证方式

- Chrome 151
- 登录 Boss 直聘
- 打开职位列表
- 打开职位详情
- 执行自动投递
- 确认职位状态正确更新
```

如果无法进行真实环境验证，请在 PR 中说明原因。

---

## Bug 修复

如果条件允许，建议提供：

- Bug 复现步骤
- 修改前行为
- 修改后行为
- 验证结果

例如：

```markdown
## Bug

职位已经投递过，但扩展仍然显示为“可投递”。

## 原因

目标页面在某些情况下不会立即更新 status 字段，
实际状态需要从另一个 DOM 节点判断。

## 修复

增加对该节点的判断。

## 验证

使用已经投递过的职位测试，状态能够正确识别。
```

---

## 不要为了测试而制造复杂 Mock

如果一个功能高度依赖：

- 真实浏览器
- 真实 DOM
- Shadow DOM
- Boss 直聘页面
- 浏览器扩展 API

不需要为了追求测试覆盖率而创建大量复杂 Mock。

如果真实浏览器环境测试更加可靠，可以直接进行手动验证。

---

## Pull Request 检查清单

提交 PR 前，请自己检查：

```markdown
- [ ] 这个 PR 是否只解决一个主要问题？
- [ ] 如果是新功能，我是否先创建了 Issue 并进行了讨论？
- [ ] 当前实现是否符合 Issue 中讨论的方向？
- [ ] 是否包含与当前问题无关的修改？
- [ ] 是否存在可以删除的重构？
- [ ] 是否存在不必要的抽象？
- [ ] 是否存在只使用一次却被提取出来的函数？
- [ ] 是否存在没有实际价值的注释？
- [ ] 是否引入了不必要的依赖？
- [ ] 是否进行了不必要的依赖升级？
- [ ] 是否进行了无关的格式化？
- [ ] 修改规模是否与问题规模匹配？
- [ ] 我是否理解 PR 中的全部代码？
- [ ] 我是否进行了合理的验证？
- [ ] PR 描述是否清楚说明了修改内容？
```

---

## 一个好的 PR

例如问题：

> 职位详情页在某些情况下无法识别职位已经投递。

一个合理的 PR：

```text
fix: 修复职位详情页投递状态识别

问题：
部分职位的 status 字段不会及时更新，导致扩展错误地认为职位仍然可以投递。

解决：
增加对页面中实际投递状态节点的判断。

修改：
- src/content/job.ts
- src/utils/job.ts

验证：
在已投递职位和未投递职位上分别测试。

其他修改：
无。
```

修改：

```text
2 files changed
+32
-8
```

这种 PR 通常比较容易 Review。

---

## 一个不好的 PR

例如：

```text
feat: 优化职位投递系统
```

然后：

```text
37 files changed
+1842
-1260
```

内容包括：

```text
- 修复职位状态
- 新增自动刷新
- 新增职位过滤
- 新增新的 AI 接口
- 重构 Job 类型
- 重构 API
- 新增 JobService
- 新增 JobManager
- 新增 JobFactory
- 修改 UI
- 更新依赖
- 修改 Tailwind 配置
- 格式化整个项目
- 增加大量注释
```

即使每一项单独来看都可能有价值，这个 PR 仍然不适合作为一个整体提交。

应该拆分成多个 Issue / PR。

---

## 关于“大型 PR”

大型 PR 并非禁止。

如果一个功能本身确实需要修改大量代码，可以提交大型 PR。

例如：

```text
feat: 重构整个职位投递流程
```

如果确实需要修改：

```text
30 个文件
2000 行代码
```

这是可以接受的。

但 PR 描述应该解释：

```markdown
## 为什么需要这么大的修改？

当前投递流程存在多个模块之间的强耦合，
无法在不调整核心结构的情况下支持新的投递流程。

因此需要同时修改以下模块：

- ...
- ...
- ...
```

原则不是：

> PR 必须很小。

而是：

> **PR 必须尽可能小，并且每一项修改都应该有明确理由。**

---

## 关于代码重构

本项目**欢迎高质量的重构**。

但重构应该有自己的目的。

例如：

```text
refactor: 简化职位状态判断逻辑
```

这是一个合理的独立 PR。

如果一个模块确实存在：

- 重复代码
- 难以理解的逻辑
- 不必要的复杂度
- 明显错误的抽象
- 难以维护的结构

欢迎提出 Issue 或 PR。

但不要把它隐藏在一个功能 PR 中。

---

## 维护成本优先

本项目是长期维护的开源项目。

代码被合并之后，不是贡献者写完就结束了。

维护者未来可能需要：

- 阅读
- Debug
- 修改
- 适配网站变化
- 处理浏览器兼容性
- 更新依赖
- 处理用户反馈

因此，一个功能即使“能够工作”，也不代表它一定值得合并。

例如：

```text
收益：帮助极少数用户解决一个非常特殊的问题

代价：
- 增加一个新的抽象层
- 增加两个依赖
- 增加大量代码
- 增加新的配置项
- 增加长期维护分支
```

这种情况下，维护者可能选择不接受。

这不是对贡献者工作的否定，而是项目维护中的正常取舍。

---

## 功能有用 ≠ 一定会合并

请理解：

> **技术上可行的功能，不一定适合这个项目。**

一个 PR 可能：

- 功能完全正常
- 代码没有明显 Bug
- 测试也通过
- 实现方式也合理

但维护者仍然可能拒绝合并。

原因可能包括：

- 项目定位不需要
- 用户需求不足
- 使用场景太少
- 增加维护成本
- 增加代码复杂度
- 与未来方向冲突
- 已经有更简单的实现方式
- 维护者不希望支持该功能

贡献者可以提出建议，但：

> **项目最终的功能范围和技术方向由维护者决定。**

---

## 维护者的 Review 权限

维护者会尽可能认真处理 Issue 和 Pull Request。

但是，由于项目是个人/小规模维护的开源项目，维护者的时间是有限的。

因此：

> **维护者没有义务对每一个 PR 进行完整的逐行 Review。**

如果 PR 存在明显问题，例如：

- 未经讨论的大型功能
- 大量无关修改
- 巨大的 AI 生成代码
- 明显的过度重构
- 不符合项目方向
- 不必要的依赖
- 明显增加维护成本
- PR 描述无法说明修改目的

维护者可以：

- 要求拆分
- 要求重新实现
- 要求删除无关修改
- 要求先创建 Issue
- 暂停 Review
- 关闭 PR

---

## 不符合贡献指南的 PR

如果 PR 明显不符合本指南，维护者可以直接关闭 PR。

例如：

```text
这个 PR 同时包含多个独立功能和大量无关重构，
请拆分后重新提交。
```

或者：

```text
该功能尚未经过 Issue 讨论。
请先创建 Issue 讨论需求和实现方案。
```

或者：

```text
当前修改范围远超该问题所需范围，
请移除无关修改后重新提交。
```

维护者**不需要先对几千行代码进行完整 Review，再指出 PR 不符合贡献规范。**

请在提交 PR 之前尽量降低维护者的 Review 成本。

---

## 为什么要求这么严格？

因为：

> **开源项目的贡献不只是“写代码”，还包括让代码能够长期被维护。**

一个 20 行的高质量修改，可能比 2000 行功能代码更有价值。

一个维护者不需要的功能，即使实现得非常漂亮，也可能没有合并价值。

一个简单的问题，如果被复杂的抽象和大量重构包围，会让未来所有维护者都付出额外成本。

因此，本项目更看重：

```text
正确的问题
    ↓
正确的方案
    ↓
最小的修改
    ↓
简单的代码
    ↓
低维护成本
```

而不是：

```text
代码越多
    ↓
架构越复杂
    ↓
抽象越多
    ↓
功能越多
    ↓
越有价值
```

---

## 给 AI Coding Agent 的特别说明

如果你使用 AI Agent、Coding Agent 或其他自动化工具协助贡献，请将本文件视为本项目的贡献约束。

在修改代码之前：

1. 阅读本 `CONTRIBUTING.md`
2. 理解当前 Issue 的目标
3. 不要擅自扩大需求范围
4. 不要添加 Issue 中没有要求的功能
5. 不要进行无关重构
6. 不要为了抽象而创建额外函数
7. 不要创建只使用一次且没有明显必要的 wrapper
8. 不要添加无意义注释
9. 不要为了“最佳实践”引入额外复杂度
10. 不要擅自升级依赖
11. 不要格式化无关文件
12. 修改完成后检查 diff
13. 删除所有与目标问题无关的修改
14. 确认自己能够解释所有修改
15. 最终 PR 应保持尽可能小

尤其需要注意：

> **不要自行扩大 Issue 的需求。**

如果 Issue 只要求：

```text
修复职位状态判断
```

不要自行实现：

```text
修复职位状态
+
重构职位系统
+
增加缓存
+
增加新的状态管理
+
增加新的 API
+
增加错误处理框架
```

如果你发现这些修改确实有必要：

> 停止实现，回到 Issue 中讨论。

---

## 最后

我们欢迎真正有价值的贡献。

你不需要提交大量代码，也不需要为了证明贡献价值而增加复杂功能。

一个很小但准确的 Bug 修复，一个简单的代码优化，一个能够降低维护成本的改进，都非常有价值。

如果你准备贡献代码，请优先考虑：

> **这个修改真的有必要吗？**

然后：

> **有没有更简单的实现？**

最后：

> **如果未来由别人维护这段代码，他会希望我现在这样写吗？**

如果答案是肯定的，欢迎提交。

如果不确定：

> **先开 Issue，讨论之后再写。**

感谢所有认真参与项目建设的贡献者。
