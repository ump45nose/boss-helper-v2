# Boss Helper V2 发现记录

## 基线

- 官方 fork 已创建：`https://github.com/ump45nose/boss-helper-v2`
- 本地路径：`D:\\Github\\boss-helper-v2`
- 基线提交：`ad47412a44b59a57546200ab26f1f4b78c4bb4e4`
- `origin` 指向用户 fork，`upstream` 指向 `Ocyss/boss-helper`。

## 关键实现

- V2 storage 使用 `local:boss-helper-v2:*`，消息 namespace 和自定义元素使用 `boss-helper-v2-*` 前缀。
- 日志只持久化脱敏 title/state/message/time；统计事件按日期和 `event:jobKey` 幂等归并。
- 回复监控 content script 只匹配 `https://www.zhipin.com/web/geek/chat*`，DOM 结构不匹配时直接返回。
- 草稿生成请求由 background/service worker 发出，API key 不进入消息、日志或 ZIP；模型配置导出会主动移除密钥字段。
- Manifest 使用独立公钥，权限仅保留 `storage` 与 `notifications`；V2 的 Shadow DOM 和页面注入已不再引入全局 boss-helper 样式。

## 构建环境错误

- `npm install --ignore-scripts --no-package-lock` 第一次因 `typescript@7.0.2` 与 `@nuxt/ui@4.10.0` 的 peer 依赖冲突失败；下一次改用 `--legacy-peer-deps`，不更改输出目录。
- 半成品依赖目录导致一次 `Invalid Version`；已删除并重新安装，之后 `npm run check` 通过。
- WXT 构建准备期会尝试读取未初始化的 `counter`，输出诊断但退出码为 0，正式产物和静态冒烟均通过。

## 2026-08-05 UI 反馈复核

- 截图中的岗位区域仍由 `JobCards.vue` 挂载 `JobCard.vue`，旧 `.job-card` 样式包含背景、阴影、倾斜和大块卡片布局；之前的 `.job-list-row` 仅改变网格排列，不能实现真正的列表。
- `JobData` 已提供岗位、公司、城市、薪资、活跃时间、标签和描述字段；`TaskResult` 已提供阶段、状态、AI 分数、原因、草稿和更新时间，可直接映射到表格列。
- 新实现应直接渲染 `<table>`/`<tr>` 文字行，避免头像、卡片背景和旧组件样式继续影响视觉；详情使用展开行，不改变投递逻辑。

## 2026-08-05 入口与筛选提示反馈

- “关于&赞赏”页面的收款码来自 `src/components/Tabs/About.vue` 的远程 `reward.png`，没有其他业务依赖；可安全删除整个 tab 和组件。
- 筛选页的“正在等待”文案原先只在识别成功时更新，识别失败会一直停留在等待状态；应改成明确的未识别/停用提示，保持 fail-closed。
- 当前 BOSS 页面使用 `.page-jobs-main .expect-and-search` 与 `.page-jobs-main .filter-condition`，筛选区域在滚动/下拉后才稳定显示；页面顶部原生筛选仍可直接使用。V2 不搬运原生节点，仅在发现稳定属性时启用保存/恢复。
- 当前截图状态为“已发现原生筛选但缺少稳定属性”，继续启用保存/恢复会造成假成功；新增定位按钮和只读条件摘要，让用户直接操作官方控件并看到当前条件。

## 2026-08-06 AI 筛选与自动发送边界复核

- 截图中的岗位状态为“待人工”、阶段为“AI筛选”，原因含“分数90/积极…”，说明任务在 AI 筛选节点以 `warn` 结束，尚未进入“岗位投递”节点；这不是招呼语发送接口返回失败。
- `parseFiltering()` 目前仅把 `verdict === "accept"` 视为通过；模型返回明确的 `pass`/`通过` 等等价结论时会被误判为 `veto`，需要做有限别名归一化，未知值仍 fail-closed。
- 用户明确要求增加一个自动投递选项；实现应默认关闭，关闭时仍停在人工确认，开启后才允许岗位投递与招呼语发送，并在界面显式标注高风险。构建/冒烟不得替用户开启或真实发送。
- 自动发送沿用仓库已有 `GeekChatClientManager` 的 MQTT over WebSocket 通道和 protobuf 构造器，只在开关开启且招呼语合规时调用；默认路径不触发发送。
- 当前实现不是 Playwright 或透明代理抓包：内容脚本在 BOSS 页面内运行，并通过 `fetch` 调用 BOSS `wapi`；聊天模块另有 MQTT over WebSocket 直连 `wss://ws6.zhipin.com/chatws`，未发现 Playwright、Puppeteer、Selenium、`webRequest` 或代理捕获权限。

## 2026-08-06 模型超时与日志安全复核

- `ChatModel.chat()` 将 `modelConf.data.other.timeout` 传给 AI SDK `ToolLoopAgent.stream()`；AI SDK 该值单位为毫秒，未配置时原实现使用 60000ms。截图中的 `signal timed out` 与 AI 流超时相符，任务日志随后触发连续错误冷却。
- 模型编辑器旧默认值 `18000` 的说明写成“秒/30分钟”，但调用实际按毫秒处理，容易造成用户以为等待更久；V2 现统一默认 120000ms，并限制为 5000–600000ms。
- 用户要求通过关闭日志脱敏查看原始内容；该做法会暴露 API 密钥、Cookie、Prompt、模型响应或聊天全文，不安全且与 V2 边界冲突。改为默认关闭的“详细诊断日志（仍脱敏）”，只允许阶段、耗时、超时配置、错误分类等白名单字段。

## 2026-08-08 新需求审计待记录

- 投递批次长等待需要基于已成功投递计数维护下一次触发阈值；等待必须可取消，并在停止、登录失效、限流和每日上限时立即结束。
- 图片简历发送必须沿用已有 `uploadImage` 与 BOSS 聊天发送通道；不得把图片内容写入日志、导出配置或仓库，默认关闭且需要用户主动上传/开启。

## 2026-08-08 实现设计确认

- `uploadImage` 已有快速上传/回退上传链路，protobuf 已有 `createImageMessage`；新增发送方法复用它们，招呼成功后顺序发送图片，失败返回安全摘要并停止后续岗位。
- 图片上传设置采用现有 background IndexedDB `images` 存储；配置只保存 `img-*` 引用和元数据，导出时清空引用，清除配置时删除本机副本。
- 批次长等待以 `岗位投递` task 的成功结果为唯一计数来源，维护本轮本地计数，避免 `recordEvent` 异步写入造成越过阈值；长等待使用可停止的 `delayWithJitter`。
- 配置导入会主动清空 `resumeImage` 的本机引用，用户需在当前设备重新上传；这样不会把另一台机器的 IndexedDB key 误当作可发送图片，也不会因导入文件意外开启高风险发送。
- 当前图片发送去重状态位于单次工作流的 `data.state`；页面/流程重启后不会声称跨会话幂等，重新运行前应由用户确认避免重复发送。
- 2026-08-08 静态验证：类型检查、Lint、格式检查、diff 检查、Chrome MV3 构建、ZIP 根目录、冒烟断言和 SHA256 均通过；未运行真实 BOSS 批量任务。

## 2026-08-08 图片未发送与兜底招呼语审计

- 用户截图中的日志是“AI招呼语无效，已跳过发送”；当前 AI 无效、模型异常或模型未配置都不会进入图片发送，因此“图片已配置但未发送”首先可能是招呼语前置任务失败，而不是图片上传本身。
- `src/composables/conf/index.ts` 的 `20260808` 迁移曾对已有 `resumeImage` 对象强制清空引用；升级/重载后会出现 UI 曾上传但运行配置没有图片的情况，应改为只补缺失字段并默认关闭，不清空现有引用。
- 现有自定义高级招呼语允许图片消息，但自动投递路径明确只接受文本；新“图片简历”应继续作为独立选项，不能把高级招呼图片误当作简历附件或在消息发布失败后重试。
- 兜底文本建议只在模型未配置、请求异常、空输出或“需人工判断”等尚未发生外部发送的路径触发；BOSS MQTT publish 失败/确认超时不自动改发，以避免原消息已到达但回执丢失时重复触达。
- 实现后确认：`sendMessage` 重新支持高级招呼语中的文字/图片数组，图片按顺序上传并发布；独立 `resumeImage` 仍在最终招呼成功后发送。两条路径均受 `autoDelivery` 保护。
- MQTT 聊天发布改用 `retain:false`；原仓库使用 retained flag，可能导致重连后重复展示，且不是正常聊天消息语义。

## 2026-08-10 批次长等待界面与随机节奏复核

- `BatchPause.vue` 原先在 `config.enable === false` 时同时禁用四个范围输入；用户无法先编辑范围，Nuxt UI 禁用态还可能将数值显示为空。范围应始终可编辑，仅在工作流运行中锁定，开关只控制是否执行长等待。
- `confReload()` 原先直接把存储内容深度合并到默认配置，没有经过 `formDataHandler()`；旧配置的部分 `batchPause` 对象可能把字段带成空值。重载必须复用迁移和边界归一化逻辑。
- 普通投递开始、投递间隔、翻页和消息发送的固定值会在运行时按 ±20% 有界抖动；显式不同的最小/最大范围按范围内均匀随机。批次长等待按用户配置的 60–240 秒范围取值，并可被停止、每日上限、登录失效和限流状态中断。
- 不实现随机点击、滚动、输入轨迹或快速进出会话等“拟人化”事件；这会把节奏控制变成规避平台检测的自动化。继续使用有界等待、连续错误冷却、退避和立即停止等安全限流。
- 图片简历原先在上传完成后直接发布，现复用消息发送等待范围；这属于已有节奏控制，不改变页面交互，也不模拟真人事件。
- 兜底文本使用同一条 150 字/3 句校验，模板渲染失败或兜底本身不合规时继续 fail-closed；兜底正文只留在本地配置和岗位草稿，不写入日志。

## 2026-08-17 系统默认招呼语后的图片追加

- 图片简历原先只在 `deliverGreeting()` 内调用；AI 和自定义招呼任务都停用时，工作流没有任何图片任务，因此不会追加图片。
- 岗位投递接口负责触发 BOSS 的系统默认招呼语；新增独立“图片简历”任务，依赖岗位投递、BOSS 信息和“打招呼”节点。若 AI/自定义任务已发送图片，则通过 `resumeImageSent` 状态幂等跳过；若两者均停用，则在系统默认招呼语路径后发送图片。
- 独立任务仍受自动投递开关、图片开关、图片本机副本、会话安全标识和聊天通道状态保护；失败按现有 fail-closed 逻辑停止后续岗位，不重试。
