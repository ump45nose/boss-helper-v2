# Boss Helper V2 Windows 安装

## 适用范围

V2 基于官方 `Ocyss/boss-helper` 的 `v0.5.1` 基线提交构建，扩展身份、DOM 前缀、消息 namespace 和存储 key 均独立，可与官方扩展并行启用。

V2 只在 BOSS 直聘页面工作。“自动投递（含招呼语）”默认关闭；关闭时不会真实投递，流程会停在“待人工”。回复监控默认关闭，开启后只发 Chrome 通知并保存待人工确认的草稿队列。图片简历发送是独立的高风险选项，默认关闭；V2 不实现自动回复或自动交换联系方式。

## 安装已构建 ZIP

1. 解压 `boss-helper-v2-0.6.0.zip` 到本机目录（不要直接在压缩包内加载）。
2. 打开 Chrome `chrome://extensions`，启用“开发者模式”。
3. 点击“加载已解压的扩展程序”，选择解压目录（目录根部必须有 `manifest.json`）。
4. 保留官方 Boss Helper，不要把两个扩展的目录混用。

## 从源码构建

```powershell
Set-Location D:\Github\boss-helper-v2
npm install --legacy-peer-deps
npm run check
npm run lint
npm run build:chrome
npm run zip:chrome
```

`typescript@6` 是 Nuxt UI 当前 peer 约束允许的版本；`--legacy-peer-deps` 只用于安装官方基线的 peer 依赖，不会写入任何密钥。

## 首次配置

1. 打开扩展选项页，在“模型配置”中填写现有 OpenAI-compatible 服务地址、模型名和密钥；模型名默认提示为 `glm5.2`，地址可使用你的现有服务（例如 `http://fn-i3h510-1.tail0292a9.ts.net:3001/v1`）。
2. 模型密钥只写入 V2 的 `chrome.storage.local` namespace，不进入源码、日志、ZIP、导出文件或回复。
3. 在 AI 页粘贴 `candidate-profile.v1` JSON（可参考 `candidate-profile.example.json`）并保存。画像为空或字段不足时，AI 招呼语会 fail-closed。
4. 先保持“回复监控”关闭；需要浏览器提醒时由用户主动开启。
5. 模型编辑器中的超时单位为毫秒，默认 120000（120 秒），可在 5000 到 600000 之间调整；测试通过不代表岗位上下文请求一定能在超时内完成。

## 自动投递开关

1. 首次安装和导入旧配置后，“自动投递（含招呼语）”均为关闭状态。
2. 关闭时，岗位仍可经过筛选和 AI 评估，但到“岗位投递”节点会停止并显示“自动投递未开启，请在配置中明确开启后重试”，不会调用投递接口，也不会发送招呼语。
3. 只有在你确认筛选、候选人画像、Prompt、每日上限和延迟范围后，才在“配置 → 招呼语配置”中勾选该开关。开启后会真实投递筛选通过的岗位；自定义或 AI 文本招呼语会在有界随机等待后通过现有 BOSS 聊天通道发送。
4. 该开关本身只覆盖岗位投递和首次招呼语；图片简历必须另行上传并开启对应选项。不会自动回复后续消息、交换联系方式或绕过验证码/风控。构建和冒烟测试不会开启这些开关，也不会使用真实账号发送。

## AI 招呼失败兜底

1. 在“配置 → 招呼语配置”启用“AI招呼失败时使用兜底语”，填写一条 1～150 字、最多 3 句话的文本；默认关闭。可配合“招呼语变量”渲染岗位字段。
2. 触发范围包括模型未配置、初始化失败、请求异常/超时、空输出、返回“需人工判断”或超出长度/句数限制。兜底文本也会经过同样的长度和句数校验，不合规时继续待人工。
3. 兜底只发生在尚未向 BOSS 发布消息的生成阶段。文本/图片消息已经发布或仅确认回调超时后，不会再次发送兜底语，避免原消息已到达但回执丢失时重复触达。

## 投递批次长等待

1. 在“配置 → 延迟配置”中启用“投递批次长等待”，填写成功投递次数范围 X～Y，以及长等待范围（默认 60～240 秒，即 1～4 分钟）。每轮达到 X～Y 中随机抽取的实际投递次数后，才进入一次长等待；下一轮会重新抽取阈值。
2. 范围输入在开关关闭时也可以编辑；开关只决定是否执行长等待，工作流运行期间所有范围会锁定。普通延迟默认在固定值上下 20% 内有界随机，批次长等待严格在你填写的最小/最大秒数内随机。
3. V2 不会随机点击、滚动、输入或快速进出会话来模拟真人，也不提供规避 BOSS 风控的行为；安全控制仅包括有界等待、连续错误冷却、限流退避、每日上限和登录失效/CAPTCHA/操作频繁立即停止。
4. 计数只包括 BOSS 岗位投递接口明确返回成功的任务，重复岗位、筛选失败、待人工和招呼语失败不会消耗批次阈值。每日上限、登录失效、验证码、操作频繁和连续错误停止条件优先级更高，等待可被停止操作中断。
5. 该功能只是保守节流，不是“防封号”；不提供代理轮换、指纹伪装、验证码绕过或其他规避平台检测能力。默认关闭，构建和冒烟测试不会开启。

## 招呼语后发送图片简历

1. 在“配置 → 招呼语配置”中上传图片并勾选“招呼语后发送图片简历”；功能默认关闭，且必须同时开启“自动投递（含招呼语）”。支持 PNG/JPEG/WebP，单文件不超过 2 MiB。AI/自定义招呼成功后会追加图片；若两者均停用，则沿用 BOSS 系统默认招呼语后追加图片。旧的“高级招呼语配置”图片消息也会按编辑顺序发送，但它与图片简历开关是两条独立路径。
2. 图片文件只保存到本机扩展 IndexedDB，配置只保存本地引用元数据；不会进入 AI 请求、日志、配置导出或 ZIP。清除图片会删除本机副本并关闭该选项。
3. 实际顺序是：岗位投递成功（BOSS 系统默认招呼语由投递流程处理）→ 若启用 AI/自定义招呼则发送文本 → 尝试上传并发送图片。图片上传或发送失败不会重试，会记录脱敏原因并停止后续岗位；已成功的投递和消息不会回滚。该发送不可在扩展内撤回，首次使用前请人工确认页面和账号。

## 超时排查与诊断日志

1. 如果岗位日志显示 `模型请求超时（120秒）`，先确认模型地址、模型名和服务端是否支持当前 Prompt 的流式响应，再按需把模型超时调高到不超过 600000 毫秒。
2. 在“配置 → 诊断配置”勾选“详细诊断日志（仍脱敏）”并保存，再只运行单个岗位。日志会显示 AI 阶段、耗时、超时配置和错误分类，关闭后不采集这些额外摘要。
3. 该选项不会也不能关闭强制脱敏；API 密钥、Cookie、完整 Prompt、模型响应、聊天全文和请求头/请求体不会写入日志。排查结束后建议关闭并清空日志。

## 筛选使用

V2 不复制或重挂载 BOSS 原生筛选节点。请在 BOSS 页面顶部/下拉出现原生筛选后直接选择城市、关键词和其他条件，再点击 BOSS 的“搜索”；V2 的“筛选”页只在识别到稳定控件属性时启用保存/恢复。当前 BOSS 自定义下拉没有稳定属性时，V2 会保持保存/恢复按钮停用，并提供“定位 BOSS 原生筛选”和只读条件摘要，避免误写错字段。

V2 的岗位过滤、HR 活跃过滤和 AI 匹配仍在岗位处理流程中执行，与 BOSS 原生搜索条件是两层独立过滤；官方 boss-helper 的筛选状态也不会自动迁移到 V2。

## 安全边界

- BOSS 登录、扫码由用户本人完成；V2 不使用 `chrome.cookies` API，不导出或展示 Cookie，也不持久化页面会话令牌。
- 页面契约不匹配、登录失效、验证码、模型超时或 AI 输出不合规时停止当前任务。
- 关闭自动投递时，招呼语和图片简历仅作为岗位详情中的草稿/配置展示；需要人工发送时仍由用户自己复制或填入并点击发送。开启自动投递后，文本招呼语和（若单独开启）图片简历才会按上述顺序走既有 BOSS 聊天通道。
- 建议先用模拟岗位或单个已登录页面受控验证，不进行批量真实发送。
