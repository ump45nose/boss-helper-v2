# Boss Helper V2 实施计划

## 目标

基于官方 boss-helper v0.5.1 创建可与官方扩展并行安装的 V2，修复日志、统计、提示词、随机延迟和筛选状态问题，并实现通知+AI 草稿监控；自动投递（含首次文本招呼语）和图片简历发送必须由用户分别显式开启，仍不自动回复后续消息或交换联系方式。

## 阶段

- [x] 1. 创建 GitHub fork、克隆到 D:\\Github\\boss-helper-v2、设置 upstream
- [x] 2. 盘点源码与依赖，确定改动边界
- [x] 3. 完成类型、存储、日志和统计基础设施
- [x] 4. 修复投递错误、随机延迟、AI Prompt 与筛选持久化
- [x] 5. 完成 V2 身份隔离、列表 UI 与通知草稿监控
- [x] 6. 单元/构建/并行安装验证，生成 ZIP、校验和和安装文档
- [x] 7. 创建提交并推送到 GitHub fork
- [x] 8. 将岗位展示改为真正的语义化文字列表并重新打包
- [x] 9. 删除“关于&赞赏”入口与收款码，并明确筛选契约状态
- [x] 10. 为不可保存的原生下拉增加定位与只读状态入口
- [x] 11. 增加默认关闭的自动投递（含招呼语）开关并修复“待人工”可观测性
- [x] 12. 修复模型超时分类并增加默认关闭的安全诊断日志选项
- [x] 13. 增加投递批次长等待配置与可观测状态
- [x] 14. 增加默认关闭的图片简历上传/发送功能
- [x] 15. 完成静态测试、构建、打包与安全边界复核
- [x] 16. 修复图片简历配置重载/发送链路并验证失败原因
- [x] 17. 增加默认关闭、可配置的 AI 招呼兜底文本
- [x] 18. 完成回归测试、重新打包、校验和与提交
- [x] 19. 修复批次长等待范围编辑与配置重载归一化
- [x] 20. 明确有界随机等待状态并复核安全限流边界
- [x] 21. 完成构建、打包、校验和、文档与提交推送
- [x] 22. 修复系统默认招呼语后的图片简历追加发送
- [x] 23. 完成默认招呼语图片路径验证、构建与推送

## 当前阶段

阶段 10 已完成：原生 BOSS 下拉缺少稳定属性时不伪造保存/恢复能力，增加“定位 BOSS 原生筛选”和当前条件只读摘要；仍不搬运节点、不点击选项、不触发搜索。新的 Chrome ZIP 已重新构建并通过静态冒烟。

当前阶段

阶段 11 已完成：增加默认关闭的“自动投递（含招呼语）”开关；关闭时明确停在人工确认，开启后才提交岗位并通过现有 BOSS 聊天通道发送招呼语。AI 结论只兼容有限的明确通过别名，未知值继续 fail-closed。已重新构建 ZIP 并更新 SHA256，未使用真实账号发送。

阶段 12 已完成：`signal timed out` 统一归类为模型请求超时，默认模型超时调整为 120 秒并限制边界；增加“详细诊断日志（仍脱敏）”选项，诊断字段采用白名单和长度限制，不能关闭密钥、Cookie、Prompt、模型响应或聊天全文脱敏。未使用真实账号进行超时回归。

阶段 13–15 已完成：增加默认关闭的 X～Y 次实际成功投递后 1～4 分钟随机长等待，并增加默认关闭的图片简历上传/顺序发送；图片只保存到本机 IndexedDB，配置导入/导出不携带本机引用。`npm run check`、`npm run lint`、格式检查、Chrome 构建、ZIP、SHA256 校验和静态冒烟均通过；未进行真实批量投递或图片发送。

阶段 16–18 已完成：保留图片配置引用、恢复高级招呼图片按序发送、关闭 retained MQTT；新增默认关闭的 AI 兜底文本设置与生成失败路径，构建/静态冒烟/校验和均通过，未进行真实账号发送。

阶段 19–21 已完成：修复批次长等待配置在未启用或重载旧配置时不可编辑/显示为空的问题，明确普通延迟与批次长等待的有界随机范围；不加入随机点击、滚动、退出等拟人化事件，不实现规避平台风控的行为模拟。类型检查、lint、定向格式检查、Chrome 构建、ZIP 和静态冒烟均通过。

阶段 22–23 已完成：AI 和自定义招呼停用时，使用岗位投递触发的 BOSS 系统默认招呼语，并在其后追加已配置图片简历；保留自动投递、图片开关和同一工作流去重守卫。类型检查、lint、定向格式检查、Chrome 构建、ZIP 和静态冒烟均通过。

## 安全边界

- 不使用 `chrome.cookies` API，不导出、展示或记录 Cookie；BOSS 请求仅沿用官方页面会话，不持久化会话令牌。
- API 凭据只允许进入扩展的 chrome.storage.local。
- DOM 契约不匹配时停止，不猜测选择器。
- 不实现验证码、指纹、代理或平台检测绕过。
- 自动投递开关默认关闭；开启后会产生真实岗位投递和招呼语发送，构建/冒烟不替用户开启或真实发送。
- 详细诊断日志默认关闭；开启后只采集受控摘要，强制脱敏始终生效。

## 遇到的错误

| 错误                                       | 尝试次数 | 解决方案                                                                    |
| ------------------------------------------ | -------: | --------------------------------------------------------------------------- |
| npm peer 依赖冲突                          |        1 | 使用 `--legacy-peer-deps` 安装，并将 TypeScript 固定到 Nuxt UI 支持的 6.0.3 |
| 半成品 node_modules 导致 `Invalid Version` |        1 | 删除已验证的生成依赖目录后重新安装                                          |
| WXT prepare 阶段 counter 未初始化诊断      | 每次构建 | 不影响构建产物；运行时由 content adapter 初始化，已记录在冒烟报告           |
| PowerShell 输出完整大 diff 超时            |        1 | 改用分文件定向检查与静态冒烟，未重复执行大输出命令                          |
